# Code Citations

## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```


## License: MIT
https://github.com/smogon/pokemon-showdown/blob/daa100485b13fb9242dfa8953a2258c79f61c6ce/server/sockets.ts

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
```


## License: MIT
https://github.com/Spandamn/DH/blob/31de40ff2ba4c74e3a8fd9cc8158bbba53aa3b3d/server/sockets.js

```
I can't edit files directly right now, but here's the complete setup for permanently fusing the client with the server:

---

## Option 1: Modify sockets.ts (Recommended)

Edit `pokemon-showdown-master/server/sockets.ts` around line 339-370. Replace the static server section:

```typescript
		// Static server
		try {
			const roomidRegex = /^\/(?:[A-Za-z0-9][A-Za-z0-9-]*)\/?$/;
			const cssServer = new StaticServer('./config');
			const avatarServer = new StaticServer('./config/avatars');
			const staticServer = new StaticServer('./server/static');
			
			// Client server - serves the Pokemon
```

